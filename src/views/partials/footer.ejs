  </main>

  <footer class="w-full bg-slate-900 text-slate-400 border-t border-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        
        <!-- Brand -->
        <div class="space-y-4">
          <div class="flex items-center gap-2">
            <img src="/img/logo.png" alt="AbastoHub Logo" class="w-8 h-8 object-contain">
            <span class="display-font text-xl font-extrabold text-white tracking-tight">
              Abasto<span class="text-brand-500">Hub</span>
            </span>
          </div>
          <p class="text-sm leading-relaxed">
            Plataforma digital para la distribución de alimentos al por mayor y menor. Abasteciendo tu hogar y negocio con los mejores precios.
          </p>
        </div>

        <!-- Links Client -->
        <div>
          <h3 class="display-font text-sm font-bold text-white uppercase tracking-wider mb-4">Comprar</h3>
          <ul class="space-y-2 text-sm">
            <li><a href="/products" class="hover:text-white transition-colors">Catálogo Completo</a></li>
            <li><a href="/products?category=frutas-y-verduras" class="hover:text-white transition-colors">Frutas & Verduras</a></li>
            <li><a href="/products?category=lacteos-y-quesos" class="hover:text-white transition-colors">Lácteos & Quesos</a></li>
            <li><a href="/products?category=almacen-y-secos" class="hover:text-white transition-colors">Almacén & Secos</a></li>
          </ul>
        </div>

        <!-- Info MVP -->
        <div>
          <h3 class="display-font text-sm font-bold text-white uppercase tracking-wider mb-4">Información</h3>
          <ul class="space-y-2 text-sm">
            <li><a href="/#ofertas" class="hover:text-white transition-colors">Ofertas Destacadas</a></li>
            <li><a href="/cart" class="hover:text-white transition-colors">Mi Carrito</a></li>
            <li><a href="/login" class="hover:text-white transition-colors">Mi Cuenta</a></li>
            <li><a href="/sorteo" class="hover:text-white transition-colors">Participar del Sorteo</a></li>
            <li><a href="/admin" class="hover:text-white transition-colors">Administración</a></li>
          </ul>
        </div>

        <!-- Contact/Social -->
        <div>
          <h3 class="display-font text-sm font-bold text-white uppercase tracking-wider mb-4">Contacto</h3>
          <p class="text-sm mb-4">
            Soporte: abastohub@gmail.com
          </p>
          <div class="flex gap-4">
            <a href="https://www.facebook.com/people/AbastoHub/61590635242288/" target="_blank" rel="noopener noreferrer" class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center text-slate-400 hover:bg-brand-600 hover:text-white transition-all" title="Síguenos en Facebook"><i class="fa-brands fa-facebook-f text-sm"></i></a>
            <a href="https://www.instagram.com/abastohub/" target="_blank" rel="noopener noreferrer" class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center text-slate-400 hover:bg-brand-600 hover:text-white transition-all" title="Síguenos en Instagram"><i class="fa-brands fa-instagram text-sm"></i></a>
          </div>
        </div>

      </div>

      <div class="mt-12 pt-8 border-t border-slate-850 flex flex-col sm:flex-row items-center justify-between text-xs gap-4">
        <p>&copy; 2026 AbastoHub. Todos los derechos reservados. MVP Validation Build.</p>
        <div class="flex gap-4">
          <a href="#" class="hover:text-white transition-colors">Términos y condiciones</a>
          <a href="#" class="hover:text-white transition-colors">Política de privacidad</a>
        </div>
      </div>
    </div>
  </footer>

  <!-- Socket.io & Notification Client -->
  <script src="/socket.io/socket.io.js"></script>
  <script>
    // Initialize Socket.io connection safely
    let globalSocket = null;
    try {
      globalSocket = io({
        autoConnect: true,
        reconnection: true,
        reconnectionDelay: 1000,
        reconnectionDelayMax: 5000,
        reconnectionAttempts: Infinity
      });

      // Request Web Notification permissions
      if ('Notification' in window && Notification.permission === 'default') {
        Notification.requestPermission();
      }

      // Notification listener
      globalSocket.on('notification', function(data) {
        console.log('🔔 Notification received:', data);

        // 1. Show emerging Toast / Alert on the page
        showEmergingToast(data);

        // 2. Show native browser push notification
        showNativePushNotification(data);
      });
    } catch(err) {
      console.warn('Socket.io client loading failed or not available in this view.', err);
    }

    function showEmergingToast(data) {
      // Create element
      const toast = document.createElement('div');
      toast.className = 'fixed bottom-5 right-5 bg-slate-900 border border-slate-800 text-white rounded-2xl p-4 shadow-2xl z-[99999] flex gap-3 max-w-sm transition-all duration-500 transform translate-y-10 opacity-0';
      
      let icon = 'fa-bell';
      let iconColor = 'text-emerald-500';
      if (data.type === 'order_confirmed' || data.type === 'admin_order_confirmed' || data.type === 'partner_order_received') {
        icon = 'fa-box-open';
        iconColor = 'text-emerald-500';
      } else if (data.type === 'order_shipped' || data.type === 'order_delivered' || data.type === 'admin_order_status') {
        icon = 'fa-truck';
        iconColor = 'text-indigo-500';
      } else if (data.type === 'raffle_code') {
        icon = 'fa-ticket-simple';
        iconColor = 'text-amber-500';
      } else if (data.type === 'commission_pending') {
        icon = 'fa-money-bill-wave';
        iconColor = 'text-emerald-500';
      }

      toast.innerHTML = `
        <div class="flex-shrink-0 w-10 h-10 bg-slate-805/80 rounded-xl flex items-center justify-center ${iconColor} text-lg shadow-inner">
          <i class="fa-solid ${icon}"></i>
        </div>
        <div class="flex-1">
          <h4 class="font-bold text-xs text-slate-100 flex items-center gap-1.5 justify-between">
            <span>${data.title}</span>
            <span class="text-[8px] text-slate-500 font-medium">${new Date().toLocaleTimeString('es-AR', {hour: '2-digit', minute:'2-digit'})}</span>
          </h4>
          <p class="text-[11px] text-slate-400 mt-1 leading-relaxed">${data.message}</p>
        </div>
        <button onclick="this.parentElement.remove()" class="text-slate-500 hover:text-slate-300 text-xs self-start p-1 transition-colors">
          <i class="fa-solid fa-xmark"></i>
        </button>
      `;

      document.body.appendChild(toast);
      
      // Animate entry
      setTimeout(() => {
        toast.classList.remove('translate-y-10', 'opacity-0');
      }, 50);

      // Auto remove after 7 seconds
      setTimeout(() => {
        toast.classList.add('translate-y-10', 'opacity-0');
        setTimeout(() => toast.remove(), 550);
      }, 7000);
    }

    function showNativePushNotification(data) {
      if ('Notification' in window && Notification.permission === 'granted') {
        try {
          const options = {
            body: data.message,
            icon: '/img/logo.png',
            badge: '/img/logo.png',
            tag: data.orderId || 'abastohub-notif',
            renotify: true
          };
          new Notification(data.title, options);
        } catch(e) {
          console.error('Push Notification error:', e);
        }
      }
    }
  </script>
</body>
</html>
