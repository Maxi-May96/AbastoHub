PORT=3000
MONGO_URI=mongodb://localhost:27017/abastohub
NODE_ENV=development

JWT_SECRET=your_jwt_secret_here
COOKIE_SECRET=your_cookie_secret_here

# MercadoPago credentials (optional, falls back to simulation if empty)
MP_ACCESS_TOKEN=

# Firebase credentials (optional, falls back to public/uploads if empty)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
